"use client";
import { useEffect,useState } from "react";
import { AppShell } from "@/components/AppShell";

export default function RunsPage(){ const [runs,setRuns]=useState<any[]>([]); const [mode,setMode]=useState("loading"); useEffect(()=>{fetch("/api/runs?brand=PF&product=PF-COOKIE-BRK").then(r=>r.json()).then(j=>{setRuns(j.runs||[]);setMode(j.mode||"live")})},[]); return <AppShell active="runs"><div className="topbar"><div><div className="eyebrow">Audit trail</div><h1>Run History</h1><p className="sub">Every module execution is versioned and approval-gated.</p></div><span className="badge draft">{mode.toUpperCase()}</span></div><section className="section"><div className="tableWrap"><table><thead><tr><th>Created</th><th>Stage</th><th>Status</th><th>Approved</th><th>Run ID</th></tr></thead><tbody>{runs.length?runs.map(r=><tr key={r.id}><td>{new Date(r.created_at).toLocaleString()}</td><td><b>{r.stage}</b></td><td>{r.status}</td><td><span className={`badge ${r.approved?"ready":"draft"}`}>{r.approved?"YES":"NO"}</span></td><td className="mono">{r.id}</td></tr>):<tr><td colSpan={5} className="muted">No persistent runs yet. Check the local PostgreSQL connection to enable the audit log.</td></tr>}</tbody></table></div></section></AppShell> }
